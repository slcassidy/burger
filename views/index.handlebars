<h1>Burgers</h1>



<div class="container justify-content-center">
    <div class="row">
        <div class="col-6">
            <h2>Available Burgers</h2>
            <ul>
                {{#each burgers}}
                {{!-- {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}} --}}
                <li>
                    {{#unless hungry}}
                    {{!-- {{hungry = false}} --}}
                    <p>
                        {{!-- {{this.id}}. --}}
                         {{this.burger_name}}

                        {{!-- <button data-movieid="{{this.id}}" class="delmovie">Devour it!</button> --}}
                        <button data-burgerid="{{this.id}}" class="eat-burger">Devour it!</button>
                    </p>
                    {{/unless}}
                </li>
                {{/each}}
            </ul>
        </div>
        <div class="row col-6">
            {{!-- <div class="col-6"> --}}

            <h2>Yum in my Belly</h2>

            {{!-- {{!-- <ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul> --}}

            {{!-- </div> --}}
        </div>

    </div>

    {{!-- Add the burger --}}
       <div class="row">
        <div class="col-12">
            
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    {{!-- <input type="text" id="ca" name="name">  Originaly Ca in the code to review --}}
     <input type="text" id="bu" name="name">
  </div>

  <button type="submit">Submit</button>
</form>
</div>
